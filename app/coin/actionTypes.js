import { name } from './constants'

export const LOAD_ACCOUNTS = `${name}/LOAD_ACCOUNTS`
export const MINT_COIN = `${name}/MINT_COIN`
export const SEND_COIN = `${name}/SEND_COIN`
export const UPDATE_BALANCE = `${name}/UPDATE_BALANCE`
